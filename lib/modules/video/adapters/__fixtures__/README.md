Provider fixture samples for video adapters.

These JSON files are fixed upstream payload examples used by
`npm run check:provider-fixtures` to validate protocol-level request builders,
response shapes, running states, and failure states for video providers.
